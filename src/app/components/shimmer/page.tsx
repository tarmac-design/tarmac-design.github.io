'use client';
import { ComponentPage } from '@/components/ComponentPage';

export default function ShimmerPage() {
  return (
    <ComponentPage name="Shimmer" description="Shimmer provides loading placeholder animations for content that is being fetched." slug="shimmer">
      <h2>Usage</h2>
      <p>See the live Storybook demo above for interactive examples and prop controls.</p>
    </ComponentPage>
  );
}
